# Preview defaults are safe; no form data is collected.
SITE_URL=http://localhost:3000
SITE_LAUNCH_APPROVED=false
FORMS_ENABLED=false
# Organization-approved HTTPS endpoint that accepts multipart form data,
# stores it durably, enforces distributed rate limits, and only returns 2xx after storage.
FORM_WEBHOOK_URL=
FORM_WEBHOOK_SECRET=
