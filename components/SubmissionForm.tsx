'use client';
import {useRef,useState} from 'react';
export default function SubmissionForm({kind,live}:{kind:'sighting'|'contact';live:boolean}){
 const sighting=kind==='sighting';const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [done,setDone]=useState(false);const [photos,setPhotos]=useState<string[]>([]);const [geo,setGeo]=useState('');const [reference,setReference]=useState('');const form=useRef<HTMLFormElement>(null);const coords=useRef<HTMLInputElement>(null);
 const currentUrl=`https://floridaflamingowg.org/${sighting?'report-a-flamingo-sighting':'contact-us'}/`;
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;setBusy(true);setError('');const data=new FormData(e.currentTarget);const files=data.getAll('photos').filter((x):x is File=>x instanceof File&&x.size>0);if(files.length>3||files.some(f=>f.size>20*1024*1024)||files.some(f=>!['image/jpeg','image/png','image/webp','image/heic','image/heif'].includes(f.type))){setError('Choose up to 3 JPG, PNG, WebP, HEIC, or HEIF photos, up to 20 MB each.');setBusy(false);return;}
 if(!live){setDone(true);setBusy(false);return;}
 try{data.set('kind',kind);const res=await fetch('/api/submissions',{method:'POST',body:data});const result=await res.json();if(!res.ok)throw new Error(result.error||'Your message could not be delivered. Please try again.');setReference(result.reference);setDone(true);}catch(e){setError(e instanceof Error?e.message:'Unable to deliver your submission. Please try again.');}finally{setBusy(false);}}
 function locate(){if(!navigator.geolocation){setGeo('Location is unavailable. Enter a location or map link below.');return;}setGeo('Requesting your current location…');navigator.geolocation.getCurrentPosition(p=>{if(coords.current)coords.current.value=`${p.coords.latitude.toFixed(5)}, ${p.coords.longitude.toFixed(5)}`;setGeo('Current coordinates added. Check they match where you saw the birds.');},()=>setGeo('Location could not be retrieved. Please enter it manually.'),{timeout:10000,maximumAge:0,enableHighAccuracy:false});}
 if(done)return <div className="form-success" role="status" tabIndex={-1}><span className="success-mark" aria-hidden="true">✓</span><p className="eyebrow">{live?'SUBMISSION RECEIVED':'PREVIEW COMPLETE'}</p><h2>{live?'Thank you for contributing.':'Your test is complete.'}</h2><p>{live?`Your ${sighting?'sighting report':'message'} has been securely saved to the Florida Flamingo Working Group sightings database. Reference: ${reference}.`:'This was a demonstration. Nothing was sent or saved, and no photographs were uploaded.'}</p>{!live&&<a className="text-link" href={currentUrl}>{sighting?'Submit a real sighting on the current website':'Send a real inquiry on the current website'} ↗</a>}<button className="button navy" onClick={()=>{setDone(false);setPhotos([]);setGeo('')}}>{live?'Start another submission':'Return to the form'}</button></div>;
 return <form ref={form} className="submission-form" onSubmit={submit} encType="multipart/form-data">{!live&&<div className="form-notice"><strong>Review mode — nothing is sent.</strong><p>You can try the form and its confirmation screen. For an actual {sighting?'sighting':'inquiry'}, <a href={currentUrl}>use the current FFWG website</a>.</p></div>}<p className="required-note">Fields marked * are required.</p><fieldset><legend>{sighting?'1. About you':'Your details'}</legend><div className="field-grid"><label>Your name *<input name="name" autoComplete="name" required maxLength={120}/></label><label>Email{sighting?' (optional)':' *'}<input name="email" type="email" autoComplete="email" required={!sighting} maxLength={254}/></label></div></fieldset>
 {sighting?<><fieldset><legend>2. Your observation</legend><div className="field-grid"><label>Date of sighting *<input name="date" type="date" max={new Date().toISOString().slice(0,10)} required/></label><label>Local time (optional)<input name="time" type="time"/></label><label className="full">Location *<input name="location" required maxLength={500} placeholder="Nearest landmark, waterbody, park, or town"/></label><label className="full">Coordinates or map link (optional)<input ref={coords} name="coordinates" maxLength={500} placeholder="Latitude, longitude, or a link to the location"/></label></div><button type="button" className="location-button" onClick={locate}>Use my current location</button><p className="field-help">Only use this if you are still at the sighting location. Your browser will ask for permission.</p><p role="status" className="field-help">{geo}</p><div className="field-grid"><label>Number of flamingos *<input name="count" type="number" min="1" max="100000" step="1" inputMode="numeric" required/></label><label>Were bands or tags visible?<select name="bands"><option value="unknown">Not sure / not observed</option><option value="yes">Yes</option><option value="no">No</option></select></label><label className="full">Band colors, numbers, or tag details<input name="bandDetails" maxLength={1000}/></label><label className="full">What were the birds doing?<input name="behavior" maxLength={1000} placeholder="For example: feeding, resting, or flying"/></label><label className="full">Additional observations<textarea name="notes" rows={4} maxLength={5000}/></label></div></fieldset><fieldset><legend>3. Photographs (optional)</legend><label className="upload-field">Add photographs<input name="photos" type="file" multiple accept="image/jpeg,image/png,image/webp,image/heic,image/heif" onChange={e=>setPhotos(Array.from(e.target.files||[]).map(f=>f.name))}/></label><p className="field-help">Up to 3 JPG, PNG, WebP, HEIC, or HEIF images, up to 20 MB each. Do not approach birds to obtain a photograph.</p>{photos.length>0&&<ul className="file-list">{photos.map((p,i)=><li key={i}>{p}</li>)}</ul>}</fieldset><fieldset><legend>4. Use of your report</legend><label className="checkbox"><input type="checkbox" name="consent" required/><span>I agree to the use of this report for research and conservation. My contact information will not be publicly shared, and exact locations will not be publicly disclosed without permission. *</span></label><label className="checkbox"><input type="checkbox" name="outreach"/><span>I also give permission to use the submitted information and photographs for education and outreach. (Optional)</span></label></fieldset></>:<fieldset><legend>Your inquiry</legend><label>Topic<select name="topic"><option>General inquiry</option><option>Research collaboration</option><option>Media request</option><option>Education</option><option>Participation or volunteering</option><option>State bird campaign</option><option>Privacy request</option></select></label><label>Message *<textarea name="message" rows={7} required maxLength={5000}/></label><label className="checkbox"><input name="consent" type="checkbox" required/><span>I understand that my contact information will be used to respond to this inquiry. *</span></label></fieldset>}
 <div className="honeypot" aria-hidden="true"><label>Leave this field empty<input name="website" tabIndex={-1} autoComplete="off"/></label></div>{error&&<p className="form-error" role="alert">{error}</p>}<button disabled={busy} type="submit" className="button navy">{busy?'Sending…':live?(sighting?'Submit sighting':'Send inquiry'):'Test the confirmation'} <span aria-hidden="true">↗</span></button><p className="field-help">{live?'Your information is shared only with the organization’s configured submission service.':'Review mode does not send or store your entries.'} <a href="/privacy/">Privacy information</a></p></form>;
}
